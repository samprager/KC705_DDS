
# When logging is enabled in the driver, this script can be run as a 
# background job, and the logfile can be examined to see the logs 
# generated by the driver.

mv logfile logfile.`date +%s`

for ((;;)) do
	sleep 1;
	dmesg -c >> logfile;
done

# Now, do "tail -f logfile" in order to view the contents continuously.
